## 2024-03-24 - Vectorizing Numpy Operations
**Learning:** Pure math operations involving numpy arrays inside Python loops (like list comprehensions) are much slower than pure vectorized numpy operations. Specifically, functions making use of `np.sin`, `np.cos`, `np.tan`, and algebraic conditions on arrays perform ~25-30x faster when refactored to use vectorized masks and element-wise array operations instead of looping row-by-row.
**Action:** When calculating grid properties over large ranges (like `linspace`), always prefer applying `numpy` conditions to arrays using boolean masking `mask = x > val; array[mask] = val2` instead of looping through `enumerate(x)`. This gives dramatic O(n) improvements and avoids Python overhead.

## 2024-05-20 - Vectorized condition evaluations in simulation solvers
**Learning:** Pure Python looping over an array combined with `if-else` branching on conditions (like checking `x/t < S_head`) inside the solver loop results in a huge performance bottleneck. Even for basic operations without numpy math functions, executing Python code for each step of an array generation causes dramatic slowdowns. Replacing this with boolean array indexing (e.g. `mask = x_t < S_head` and `rho[mask] = val`) avoids evaluating scalar conditions in python and speeds up array generation by ~40x.
**Action:** Always favor vectorized evaluation with boolean masks when sampling discrete regions. Rather than computing values row by row using conditions, evaluate all values simultaneously with `np.where` or mask arrays.

## 2025-02-27 - Vectorizing root-finders in array evaluations
**Learning:** Using scalar root-finders like `scipy.optimize.brentq` within a Python list comprehension (or `np.vectorize`) is a significant bottleneck when solving implicit functions over an array. While `brentq` is extremely robust for scalars, it does not support array operations natively. Swapping `brentq` for `scipy.optimize.newton` allows for fully vectorized root-finding because `newton` natively handles NumPy arrays. This eliminates the Python loop overhead, yielding ~10-15x speedups in the solver loop. However, since `newton` doesn't strictly bound its search, you must supply intelligent initial guesses (e.g., based on the physical regime) and check if the resulting roots cross physical boundaries to ensure convergence to the correct root.
**Action:** Whenever solving an implicit equation over an array of values, use `scipy.optimize.newton` to take advantage of array vectorization, falling back to a scalar root finder like `brentq` only in cases of non-convergence. Always provide physically-informed, vectorized initial guesses to guide the Newton solver and verify the roots stay within the expected domain.
## 2026-03-29 - Vectorized matplotlib plotting
**Learning:** When generating multiple lines in a single matplotlib plot (like an expansion fan), plotting in a loop over individual lines is slow. The `ax.plot()` function accepts 2D arrays (where columns represent separate data series). Vectorizing the data preparation and passing a 2D array avoids multiple `ax.plot` calls and reduces rendering overhead by ~15-20%.
**Action:** Instead of `for angle in angles: ax.plot(x, np.tan(angle)*x)`, compute the 2D array `Y = (np.tan(angles)[:, np.newaxis] * x).T` and call `ax.plot(x, Y)` once.

## 2026-03-29 - Providing Analytical Derivatives to SciPy Newton Solvers
**Learning:** By default, `scipy.optimize.newton` uses the secant method when only an objective function is provided, which requires additional function evaluations to approximate the derivative numerically. Providing the exact analytical derivative via the `fprime` argument forces the solver to use the Newton-Raphson method, which benefits from quadratic convergence rather than superlinear convergence. In physics calculations involving expensive operations (like trigonometric and square root evaluations in the Prandtl-Meyer function), this reduces total evaluation overhead significantly.
**Action:** Whenever using `scipy.optimize.newton` to solve implicit equations, always manually compute and provide the analytical derivative as `fprime` if possible, to maximize convergence speed and reduce computational overhead.

## 2026-03-31 - Avoiding Nested Root-Finders through Analytical Inversion
**Learning:** In the `CDNozzle.solve()` normal shock regime, locating the exact shock position requires a numerical root-finder (`brentq`) evaluating a residual constraint. Previously, this residual constraint called `IsentropicRelations.calc_mach_area()` internally, effectively spawning another nested root solver (`newton` or `brentq`) to solve the subsonic area-Mach relationship for every iteration of the outer shock-finding solver. This nested numerical evaluation pattern causes massive O(N^2) scaling overhead and makes the code over ~80x slower.
**Action:** Always verify if physical equations within a root-finding residual can be algebraically manipulated into an analytical form. By enforcing conservation laws directly (e.g., analytically computing `M_exit` based on pressure ratios and equating the required area ratio against the available effective area), we completely eliminate the inner numerical root finder. This reduces the problem complexity to O(N) and provides dramatic speed improvements without losing accuracy.

## 2026-04-05 - Replacing Numerical Optimization with Analytical Exact Solutions for Boundaries
**Learning:** When solving for bounded physical domains (like max deflection angle in oblique shocks), using numerical optimizers like `scipy.optimize.minimize_scalar` to find the exact boundary condition introduces significant evaluation overhead (~0.25s for 1000 calls vs ~0.06s). Since many of these boundary curves have known analytical maxima (derivable via calculus), replacing the numerical optimization step with the exact algebraic equation provides an instant O(1) calculation and makes the bounding step ~4x faster with zero loss in accuracy.
**Action:** Before reaching for a scalar optimizer to find peaks or boundaries of a physical relation, always check if an analytical derivative exists that can be solved algebraically for the exact turning point.

## 2026-04-10 - Optimizing high-frequency NumPy solver conditions
**Learning:** In implicit root-finding loops operating over large arrays, generic NumPy boundary checking functions like `np.isclose()` introduce significant overhead (~10x slower) compared to explicit element-wise arithmetic checks like `np.abs(a - b) <= tol`. Furthermore, when solvers like `scipy.optimize.newton` evaluate residual constraints (`func`) and their derivatives (`fprime`) hundreds of times, recalculating constant factors based on input arguments (e.g., specific heat ratio Gamma limits) scales poorly.
**Action:** When working inside inner evaluation loops of numerical array solvers, explicitly evaluate mathematical bounding conditions rather than using convenience functions like `np.isclose`. Additionally, eagerly extract and cache any invariant scalar constraints out of the solver loop's closures.


## 2026-05-18 - Inlining Functions in Solver Loops
**Learning:** In the `PrandtlMeyer.inverse_prandtl_meyer` array solver, the numerical root-finding loop repeatedly evaluated a generic helper function (`PrandtlMeyer.prandtl_meyer_function(M_guess, gamma)`) as its residual constraint. This generic function contains internal `np.asarray()` checks, shape allocations, and boolean mask evaluations (`M_arr >= 1.0`). Calling this inside a fast inner solver loop introduces severe array allocation and dynamic typing overhead on every single iteration of `scipy.optimize.newton`.
**Action:** When a numerical solver repeatedly evaluates a physics constraint, do not reuse the generic module-level API function. Instead, manually inline the pure math operations into the solver's closure and eagerly extract any loop-invariant constants. This avoids redundant array allocations and provides significant speedups without changing the underlying mathematical safety.

## 2026-06-05 - Vectorizing Class Calculations and Inlining Formulas
**Learning:** In the `Aerodynamics.newtonian_cp` calculation, instantiating a `NormalShock` object internally to compute the total pressure ratio creates significant per-element overhead and breaks NumPy vectorization because the `NormalShock` constructor uses scalar boolean conditions (e.g., `M1 < 1.0`). Passing an array triggers `ValueError: The truth value of an array with more than one element is ambiguous`.
**Action:** When a function needs to handle both scalar and NumPy array inputs, never invoke scalar-oriented class constructors inside the function. Instead, inline the pure mathematical calculations directly (like the exact Rayleigh Pitot tube formula) and apply conditions using boolean masking (`mask = theta >= 0`) instead of scalar `if` checks. This avoids ambiguity errors and provides an ~15x performance boost.

## 2026-10-25 - NumPy Bounds Checking
**Learning:** When validating boundaries on large NumPy arrays, replacing `np.any(array >= threshold)` with `array.size > 0 and np.nanmax(array) >= threshold` (or `np.nanmin` for lower bounds) avoids massive boolean array allocations in memory, providing significant speedups (~7-8x). Always check `array.size > 0` first to avoid `ValueError` on empty arrays.
**Action:** Replace instances of `np.any(array >= val)` or `np.any(array <= val)` with `np.nanmax` and `np.nanmin` combined with an `array.size > 0` check when validating data across large arrays.

## 2026-10-26 - Precomputing Constants and Inlining Functions in SciPy Solvers
**Learning:** In numerical solvers like `scipy.optimize.brentq` that evaluate a `residual` function thousands of times, calling abstracted class methods (e.g., `self._calc_f`) that redundantly perform dictionary lookups and recalculate physics constants introduces massive function call and arithmetic overhead. By inlining the calculation directly into the solver's closure and explicitly precomputing all loop-invariant mathematical terms (e.g., fractional exponents, ratio constants) beforehand, the execution time of the inner numerical loop can be reduced significantly (~20% faster) without changing any logic.
**Action:** Before passing a `residual` function to a SciPy numerical root-finder, always extract and precompute any constants that don't depend directly on the iteration variable `P`. Inline abstracted math methods if their repeated function call overhead outweighs their readability benefits inside the hot loop.
